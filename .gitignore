# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# Added by code-review-graph
.code-review-graph/
# Generated by the local code-review-graph install: CLAUDE.md and the four
# .claude/skills/ entries instruct agents to use MCP tools that only exist on a
# machine running that server, and .mcp.json hardcodes an absolute cwd that is
# wrong in every other clone and in every worktree under worktrees/.
CLAUDE.md
.mcp.json
.claude/skills/
# Personal Claude Code overrides; the shared config is .claude/settings.json
.claude/settings.local.json
# Worktrees created by branch work
worktrees/
